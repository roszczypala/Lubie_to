{% extends '::base.html.twig' %}

{% block middle -%}
    <h1>Edit photo</h1>

    {{ form(edit_form) }}

    <a class="btn btn-success btn-block" href="{{ path('photo') }}">
        Back to the list
    </a>
{% endblock %}
